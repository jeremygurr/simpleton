@n: ma?-----dd'aP`a

features:
  docker container based: runs on any os
  easier debugability: isolated cells with separate logs
  organize functionality into repos
  abstraction for:
    retry 
    parallel execution
    multi-value dim handling
    caching
    orchestration
    locking
    multi-strategy handling
    pretend execution management

docs
  cube names should be verbs, and folders which contain them should be nouns
  low folder contains low level cubes, should not generally be used directly

handlers
  look in in/ for folder patterns and execute based on them
  in/src/java compiles to out/main/classes
  in/lib/java pulls in external libs needed to out/lib/java
  in/**.bash-template get hydrated
  sourced before local context
what about supporting member lists that are too long?
  can specify a file instead of a string: @members
passing complex member lists to sub
  case: service is already set for function move, but it needs undeploy to happen, which has it's own set of services
  can't update deps until this is resolved
  optional function to calculate dimensions for a given sub: calc_sub_dims "$sub"
default freshness for single_value
  overlapping auto-expire functionality
  distinctly different: not using auto-expire can allow for fetching of old values
universal pretend var
  f, t, ask
  maybe function
up links are relative, so we can't move the cells like we need to 
  but if we make it absolute, what if another cell is trying to link to it when it moved?
  [ ] keep it relative and recompute it as needed
  [*] switch to absolute, still need to recompute
      does it make sense to put in repo with abs paths?
      simpler to read
renaming cells to verbs
  but what about dim cells?
  [ ] noun form implies get-
  [*] change dim names to get-, and let code remove the verb
  [ ] link is noun, but target cell is verb
need to track two dates?
  key was fetched but didn't change, so is more fresh
  out folder needs to have newer date
  how to track last change?
  [ ] separate file for change
  [*] out folder for change, separate file for freshness
near and far average timings for cubes
should strategies be implemented in up or as part of the cube itself?
  [*] in up
      need to have special logic in update code to handle different variations
  [ ] directly in the cube, with multiple execution functions for different strategies
      each strategy may have separate dependencies or resource needs
      need to track statistics for each strategy separately
do we support multi-strategies inside of multi-strategies?
  only one level, other cells can aggregate more levels
  much simpler
cube vs cell
  log
    cell: last success / fail
    cell: stage execution times
    cube: history of requests
  status
    cell: avg execution / error rate
  lock
    cell
can we simplify dims?
  not cubes? only a function script?
  use cases: pods and etcd keys
    keys don't really have a validation
    pods need to be able to be referenced even when in a deleted state
  how to share between cubes?
    we may want to share other files between cubes also
    just a simple link?
    can have a folder containing files even if they aren't a cube
can we simplify resources?
  unify with in deps
  provide dim=any to find the first which is available and can be locked
  normal deps don't need a lock?
  how to mark these as needing a lock?
    [*] lock- prefix
    [ ] res- prefix
type of freshness check
  [ ] flat
  [ ] shallow one level deep
  [*] full depth, skipping local cells
----------------------------------------------------------------------------------------------------
don't change a parent cell if no child cells actually changed (even if they were updated)
need to put freshness into a file which can be quickly read by update checkers, not in context file
  cube-vars, optional and sourced if it's there during update and needs-update checks
should cubes be able to specify freshness requirement of each dependency, or just use default?
  [ ] use default value
  [ ] allow overrides in a var
  doesn't apply for local cells
  cube_is_remote=t/f
  cube_is_effectively_remote (if any of it's deps are remote)
cubes must have one cell minimum. If so: only
  because of cube -> cell up linking, needs to link to a folder
go through cell-update
update other cell commands
update dna commands
handling updates
  [ ] check if a dep needs to be updated first, and then check again while updating
  [ ] immediately update it if needed
  [*] push driven modification checks, making checks trivial, but complicating changes
      checks happen a lot more often than changes
      write a 'needs-update' file to out_path with info about cell which changed
      but this won't work for freshness checks, only for change checks
      maybe we can track min freshness also
      needs-update
        change_cause=...
      min-upstream-freshness
        min_upstream_freshness=...
should fresh requirement ever be deep?
  [ ] only apply to top level and allow all other cells to use their defaults
  [*] + add a fresh_deep option
should we make up cell specific, auto attaching to specific cells?
  could make return path not needed
  simplifies down stream pushes on change
  can't link to a specific cell though?
    link to out path, from which cell id can be derived
  do dims or res need to be cell specific?
    dim and res changes need to require updates of downstream
    do we even track downstream of dim/res?
      need dim/up dim/down
      rename up to something else: 
      [ ] factor
      [ ] part
      [*] in (combine with source somehow?)
instead make a folder 'cell' that all cell ids are under with their own
  in/res/out/log/status folders
will need to hold lock for upstream cells while updating down
how to aggregate many cells of a cube into a single document
  an upstream group can have many cells
how to handle orphaned dim members (pods which which were deleted)
where will alternate strategy retries happen?
  how does needs_update work for multi-strategy deps?
    [ ] assume they don't need updating
    [ ] if at least one dep doesn't need updating
    [*] if any dep is changed, needs update
  happening during dependency execution
  loop over each strategy in order until one succeeds
operation_scope=long / short
  should change multi-select strategy
  look at long values instead of short?
  randomization to keep strategies from becoming blocked
  retry should choose a different method?
get etcd value
  path: one-data/etcd/value
  value: env project key 
  derived: service_domain, certs
get-folder-keys-from-dump
get-folder-keys-direct
get-folder-keys
  choose most reliable then fastest?
write-key
mvs/function/execute
mvs/function/undeploy
mvs/function/load
mvs/function/deploy
deploy fsm 
  # need to set up tunnels beforehand (maybe 3 tunnels?)
  up
    01-undeploy-function-from-target
      up
        functions-endpoint
    02-deploy-function-to-target
      res
        openshift-controller-tunnel
    03-update-etcd
      up
        etcdctl-setup
          up
            etcd-certs
            etcd-endpoint
    04-confirm-envoy-update
      up
        access-to-envoy-config
    05-undeploy-from-source
      up
        functions-endpoint
top level of workspace should be separate repos
  need a way to specify dependencies?
    just validate up links and if missing, give instructions
    or just use domain names like github
validation of dims even for edit command
support testing and mocks
  mock folder contains output for various mock flags
  test folder contains test cases
support adding keys
ecp/
  openshift-tunnel/
one-data/
  etcd/
    service-access/
      dim/ 
        # dimension names must be singular, since it can be easily changed to plural later
        env/
          in/
            get_members
              get_members() {
                members="e1 e2 e3"
              }
          out/
            possibilities?
            functions?
              expand_dim
      in/{e1,e2,e3}
        certs/
          *.crypt
        endpoint/
      out/{e1,e2,e3}
        certs/
        endpoint/
    as-file/
      up/
        server-access/
    function/
      current-version/
        up/
          versioner/
      lambda-json/
        up/
          etcd/key/
      down-dependency/
        up/
          current-version/
          lambda-json/
      up-dependency/
      service-map/
      dependency-service-map/
      shallow-dependencies/
      deep-dependencies/
    key/
  mvs/pod/function/loaded-version/
  mvs/pod/function/is-loaded/
    # if fresh is set, will check out folder against that limit
    # optional (if missing, will NOT be created)
    # this folder timestamp is updated if any deps are modified
    up/
      # 'any' folders. Any of the cubes inside may be used to satisfy dependency
      *?/
    # auto created and updated when a dependency is added with the 'cell add up' command
    down/
    # just like up dependencies, except they are temporarily allocated resources
    res/
    dim/
      {name}/
        # should not do anything cell specific, but may have functions which do
        cube-context
        out/
        out/members  # optional
    # if no in folder, no internal check for freshness
    in/*/
      # special files that are encrypted
      secret/
    # any of these can be vectorized or not
    # is NOT saved in repo
    out/*/
      *.new/
      *.old/
      # is NOT cleared after cell update completes
      secret/
    # where new data should be written to
    # is cleared after cell update completes, created if needed
    # ignored by repo
    tmp/*/
    # if this doesn't exist, logs won't be written
    # logs can be cell or cube level
    log/*/
    # detailed debugging info
    debug/*/
    status/*/
    # if this doesn't exist, locks won't be held. Cell is assumed to be effectively immutable
    # locks can be cube or cell level, selected by cell_logs=t
    lock/*/
    # used to unlock secrets
    key/
    # can't be vectorized because it's a file
    cube-context # used when only status is being checked
      measures="versions"
      expand_dimension() {
        # expand_dimension {dim_folder} {dim_plural} {value}
        # unaliases
        # expands
        # sets defaults
        # returns true or false depending on whether dimension members are valid
      }
      execute_update() {}
debug_log=/aoijdfija/dfoijioajsd
will output debug trace logs to this folder
help command
each folder has a README, created from the template
new command  
edit command
status command
list of commands
auto-complete
verify lib behavior
compile
test

