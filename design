@n: ma?-----dd'aP`a

handlers
  look in in/ for folder patterns and execute based on them
  in/src/java compiles to out/main/classes
  in/lib/java pulls in external libs needed to out/lib/java
  in/**.bash-template get hydrated
  sourced before local context
what if we want to output to screen and cache result?
  ( ) write_to_stdout write_to_cache write_to_parent
      write_to_stdout defaults to true if not writing to parent
  (*) cube_update doesn't bother with output, but the cell command may auto view
      view folder contains various renderers of the output data
      built in viewers: view=raw view=none
      this means out must always exist, but may have a cache limit?
make expire_after variable in cube_context
  check expiration at beginning of cube update and delete all expired data
no more save_path, instead auto detect from cube path parent cube
  symbolic link from out$cell_id to {parent}/return${parent_cell_id}/{part}/{name}
  delete the return link when cell completes successfully
what about supporting member lists that are too long?
  can specify a file instead of a string: @members
passing complex member lists to sub
  case: service is already set for function move, but it needs undeploy to happen, which has it's own set of services
  can't update deps until this is resolved
  optional function to calculate dimensions for a given sub: calc_sub_dims "$sub"
can't pass parent_return_path into cube_update without a cell_id
  so what goes into cube_update then? Or is this something that should go into cell_update?
  ( ) compute cell id earlier? 
    can't compute it before pivot
  ( ) make quick check after pivot?
    but then we can't do parallel decision in cube_update
      would need to be pushed into pivot, but then pivots are already parallel then?
  (*) quick check doesn't apply when there are dims anyway, so cell_id is known
parent return path must include cell id (doesn't seem to be for single-value cube)
  actually, no cell id for dims anyway, so this is ok
how to handle debug_search reverting to searching when exiting a subshell?
  need to record debug state into a file
    during interactive debug loop
    only record if a significant change happens
    write atomically
  read from file
    at beginning of debug trap
    will slow down debugging a lot
    + check date of debug file?
  make this work for multiple threads?
    should only need a global state
      what we are searching for
      watches
      basic settings
    only save var when change is made
      keep a list of vars which have changed since last fork?
  load_debug_state
    when pid changes
      debug_prev_pid= can't work because var isn't maintained between processes
      need to read a current_pid file
        no just keep track of a last_load_state time and compare to debug_state file
  save_debug_state
    when user changes something
cube_update should not return false for update needed, instead use var
  but cube_update always runs in subshell, so var passing is impossible
  [ ] use file passing
  [ ] use different function to determine if update is needed
  [ ] don't run in subshell if checking 
  [ ] use y=$(g 10>&1); eval "$y"
      this works for single thread subshells but what about multi-thread?
      each starts with a separate fd
      but they'll be writing to the same vars?
      only use for update check which only uses one thread
  [*] use different return code
if out contains a output file, auto display?
simplify locks
  parallel pivoting and many->many subs makes it impossible to pass in fd to locks
  immutable structures as much as possible
  use out folder as cell lock
  make new$cell, if failed, wait a bit and check again
  new$cell.pid = process locking new
  how to read lock cells
    (*) hard link files to parent cube return
        cp -alu 
        while write lock is being held
        handle large slow changing folders more efficiently:
          just link the out folder for static cells
          dynamic cells will hard link
          cube_change_class=dynamic / static (default dynamic)
          cube_cardinality=low / high (default low)
          cube_expiration (replace expire_after)
    ( ) use separate locking mechanism
  locking resources 
    new folder for this, linked to lock folder of parent cube
put subshell back to cube
making ultra-light cubes?
  lots of cubes even for simple cubes because of dims
  impact
    subshell
    load context
    feature setup
  cube_update does lightweight check first
    needs to compute cell id still?
      can check the return folder
    if context < out
    if fresh
    if no dims or deps or locks
unnecessary updates
----------------------------------------------------------------------------------------------------
faster needs_update check for dims
default freshness for single_value
  overlapping auto-expire functionality
build retry mechanism
  retry_max=1, retry_delay=1, retry_scale=2
  update returns 2 if retryable
default freshness for etcd keys
get etcd value
  path: one-data/etcd/value
  value: env project key 
  derived: service_domain, certs
single key fetch: one-data/etcd/single-value-loader
cached version of etcd key: one-data/etcd/dump-loader
universal apply_changes var
  f, t, ask
deploy fsm 
  # need to set up tunnels beforehand (maybe 3 tunnels?)
  up
    01-undeploy-function-from-target
      up
        functions-endpoint
    02-deploy-function-to-target
      res
        openshift-controller-tunnel
    03-update-etcd
      up
        etcdctl-setup
          up
            etcd-certs
            etcd-endpoint
    04-confirm-envoy-update
      up
        access-to-envoy-config
    05-undeploy-from-source
      up
        functions-endpoint
validation of dims even for edit command
support adding keys
don't log trace steps
folder
ecp/
  openshift-tunnel/
    
one-data/
  etcd/
    service-access/
      dim/ 
        # dimension names must be singular, since it can be easily changed to plural later
        env/
          in/
            get_members
              get_members() {
                members="e1 e2 e3"
              }
          out/
            possibilities?
            functions?
              expand_dim
      in/{e1,e2,e3}
        certs/
          *.crypt
        endpoint/
      out/{e1,e2,e3}
        certs/
        endpoint/
    as-file/
      up/
        server-access/
    function/
      current-version/
        up/
          versioner/
      lambda-json/
        up/
          etcd/key/
      down-dependency/
        up/
          current-version/
          lambda-json/
      up-dependency/
      service-map/
      dependency-service-map/
      shallow-dependencies/
      deep-dependencies/
    key/
  mvs/pod/function/loaded-version/
  mvs/pod/function/is-loaded/
    # if fresh is set, will check out folder against that limit
    # optional (if missing, will NOT be created)
    # this folder timestamp is updated if any deps are modified
    up/
      # 'any' folders. Any of the cubes inside may be used to satisfy dependency
      *?/
    # auto created and updated when a dependency is added with the 'cell add up' command
    down/
    # just like up dependencies, except they are temporarily allocated resources
    res/
    dim/
      {name}/
        # should not do anything cell specific, but may have functions which do
        cube-context
        out/
        out/members  # optional
    # if no in folder, no internal check for freshness
    in/*/
      # special files that are encrypted
      secret/
    # any of these can be vectorized or not
    # is NOT saved in repo
    out/*/
      *.new/
      *.old/
      # is NOT cleared after cell update completes
      secret/
    # where new data should be written to
    # is cleared after cell update completes, created if needed
    # ignored by repo
    tmp/*/
    # if this doesn't exist, logs won't be written
    # logs can be cell or cube level
    log/*/
    # detailed debugging info
    debug/*/
    status/*/
    # if this doesn't exist, locks won't be held. Cell is assumed to be effectively immutable
    # locks can be cube or cell level, selected by cell_logs=t
    lock/*/
    # used to unlock secrets
    key/
    # can't be vectorized because it's a file
    cube-context # used when only status is being checked
      measures="versions"
      expand_dimension() {
        # expand_dimension {dim_folder} {dim_plural} {value}
        # unaliases
        # expands
        # sets defaults
        # returns true or false depending on whether dimension members are valid
      }
      execute_update() {}
debug_log=/aoijdfija/dfoijioajsd
will output debug trace logs to this folder
help command
each folder has a README, created from the template
new command  
edit command
status command
list of commands
auto-complete
verify lib behavior
compile
test

