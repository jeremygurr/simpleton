@n: ma?-----dd'aP`a

auto aggregation?
  project/env/service/pod/info
    pod can get it's own info, but more efficient if parent has it
    project/env level is best for fetching info
  done through multiple choice
how to make multiple processes depend on a shared lock
  must depend on a shared cell
  utility cell?
  ability to lock state of a cell during operation
  introduces new concept: write locks instead of read locks
    just make an unlock_when_done=f parameter
if obtaining a lock it already owns, fail because of circular
launching stages in parallel
  cell_max_runners=10
    checks cell processes when starting a cell, and will wait if past the given amount
  cell_max_idle_requirement=50
  cell_max_load=10
what if you want to organize the same data in different hierarchies?
  env/project/service
  project/env/service
  where should the locks and context come from?
    [*] unlinked path
    [ ] linked path
        must be this otherwise parent locks that came from the linked version won't unlock for the child
cell edit command
  to edit a file and touch the cell folder
  could potentially open secure files
auto management of secret files
  something.secret
  edit automatically manages
  edit of a blank file will create a new master key for the new file
  add-key adds a key to an existing secret
status dependencies may be different than update dependencies
  status of pod list requires oc get pods, but update requires ssh to osm
  no, status is not about reading remote data, but always reporting on immediate information stored locally, never requiring a lock
dependencies
  freshness requirement?
  value requirement: oc project
need to break cells into smaller pieces
  read and write parts should actually be separate cells
optional locking? 
  for static values in particular
  read only lock for reading / status
  locking always happens for cells
static values
  mvs/service/list
    cell-info/
    out/context      # script containing get_out function
all/any consistency
  [ ] require count level: -any, -all for each
  [*] count level optional: -any, -all, -none, -2, -3, anything else is single cell
  [ ] have a separate config file
etcd key vs etcd blob file: separate cells
links should be relative so structure could be saved?
  not need if run in docker container
  but it does make it more moveable
cell as a non-caching service vs cell as a cache
  transient cell vs persistent
  should we even allow this?
  need to be able to clear out caches in an intelligent and automated way
    based on cost-tier
    low cost, oldest first, until it is cleared, then next tier
    cell free command will do this
  cost_tier defined by cell
    0 is no cost, and isn't cached at all
    1 is lowest cost to be cached
    2...
should multi-cells on the commandline also run in parallel?
  no, explicitly serialize them
we must also parallelize multi-row updates
  when computing key, must split up all
    also other aliased groups of members (e3, e3i)
  need way to compute all members of a dimension
do we need read locks of deps before write lock of cell?
  how do we hold the read locks? Are we stacking many forked processes to do it?
  yes, but can we do it without forking?
  write lock can be obtained after determination of change needed
how to fetch dim members
  many are constants, but some can be dependents on cells
  [ ] always require member list dependencies
  [ ] don't need list of pods unless all is chosen
  [*] each dimension has it's own cell
        must support ultra-light cells
ultra light cells
  nothing is actually required
how to handle dimension expansion?
  for all, need list of members
  for any, could need list or not, depending on how it handles it
    use a different cell for this, with it's own validation
do we need to load the full context for each dim cell and up cell?
  need to separate update context from status context
  status context must not change vars, just add some functions (no defaults?)
supporting nested any/all cells
  something/
  something?/
    g1/
      c1/
      c2/
      g3?/
        c6/
        c7/
      g3/
    g2/
      c4/
      c5/
do we need to recursively lock deps?
  no, only one level
  just because function execute requires osm tunnel, what needs the function execution doesn't
can a cell row depend on more than one cell row of another?
  usually the other way around
  support only one->one or many->one deps for now
phases
  context
  dimension
    normalize
    validate
  for each row:
    freshness / change check
      does change check need to see if deps have changed?
        for compiler type actions, a small change should trigger an update
        make the change push it out
    dependency 
      lock read for each
      update 'down' dependencies if needed
    lock write
    execution
how to handle recursive cells like function-dep-web
  either produce all mappings in a single cell row, or make a on-demand generator of the info
  but no support for dynamic dependencies
pull new status from cell_update_after_write_lock
should write new data to out.new and move at end
  only move out.new to out if it's actually changed
add status command
  convert touches to file writing with timestamp
  stages
    loading status context
    waiting for locks
    waiting for deps
    waiting for execution
after completion:
  move log to last-success or last-failure
  update status
  update execution log
how to handle shared use of etcd certs
  we don't want to expand them over and over
  read lock then expand?
  secure folder holds encrypted files
  expands to out/tmp
folders ignored by git:
  out/*       # this means out must not be source of truth for anything (use in/)
  down/
  tmp/
  log/*
  status/*
how to avoid duplication of README.md?
  template folder in parents or extensions
cp in to out
  in is optionally row specific
auto create ssh key in simpleton home
record who called a cell-update
  pass var into update: called_from
  status
  history
in/*.crypt
  unlock before execution of update
make decrypt_file in common
make workspace in ram disk
push out to github
where are dimension cells updated?
  they must be updated before dep update can happen
	dim cells must be updated in hierarchal order
dimensions for function dep analysis
	one row in a cell may map to multiple dep rows (pulling multiple keys from etcd)
normalize_dimensions can't happen until dim cells are updated
  need to normalize as we are updating
  update first dim, with no inputs, then normalize first dim
  update second dim with first dim inputs, then normalize second
  load status must be before normalization
  can't update row set until dims are updated
a dim cell can have dims
dim expansion should be described by dim cell
  out/functions
  possibilities
  expand_dim
finish any_cell functions
volume=high / low : allow selection of different strategies
  high: cache full etcd
  low: cache only one key
tracking broken strategies
  cache break for a certain amount of time
how can volume=high favor cached option?
  put cached version first, and make it invalid if volume=low
    unless it already has an adequate cache loaded?
    can be decided by cache manager when validation function is executed
how can an upstream any dep be chosen differently by different rows?
  ( ) vectorize up
  (*) choose which path to take each time update happens
      does required row exist in this cell?
      does fresh cache exist?
      is this ok for volume level?
      needs to set a variable with the right path chosen
can we avoid caching for some cells?
  for high cardinality dimensions: etcd key fetching
  dont_cache_result=t/f/d
  update decides if it writes anything to out
  update should return result
  get_from_cache function 
  get_cache_freshness function
verify = before / after / both
test = none / quick / balanced / deep
autorepair = t
integrate intend/expect/observe into base cells
  don't really need expect, just allow update to be the fix command
supporting high volume cubes output/locks/logs/etc
  locks should just be files that get deleted when done
    will create race conditions. Better to just keep the file around
    cell_locks=t (default)
  historical logs should not be in separate folders but aggregated across the whole class, only last success / failure across all kept. while running logs are for each instance
    cell_logs=f (default)
  very large dirs
    use simple names first, and then hash if too many / simple name can't be found
    configure as high_volume=t for cell
      determines if we use hashes
what is required for a cell?
  context-status
do we really need separation between update and status?
  only have update context
terminology: cell vs cube
  cube contains many cells
terminology: static cell vs dynamic
  static cell is only ever changed locally, so freshness is irrelevant. As long as source isn't changed more frequently, it never needs to be updated
all cell info should only be user readable
need to make the expand function localized somehow
  but how do we get output from a cell? must use out folder
  subshell
static vs dynamic cubes
  process is fairly different
  (*) keep same process, just control using default freshness (unset means no requirement)
  ( ) split into two processes: static vs dynamic
      static doesn't need to check or update freshness
      not really that big of a deal?
lower level cubes organized into low/ folders, so they aren't visible as part of high level
for simplicity: no dirs are auto-created
  if they are required, the update should fail with the proper message
  try not to require anything
before launching parallel processes: wait for process count / load to be low enough
  use a standardized function for this
both physical and logical context read?
  if physical and logical parents are different, do physical first then logical
how to support multi-stage cubes
  ( ) chained dependencies
      how can we skip steps in this case?
  ( ) aggregation cube with stages
      easier to follow
  (*) all cubes have aggregation support
      parallel execution is already supported with normal deps (up/)
      if up folders are prefixed with a number and hyphen, will execute those sequentially, others will execute in parallel
        01-undeploy-from-target
        02-deploy-function-to-target
        03-update-etcd
        04-confirm-envoy-update
        05-undeploy-from-source
      parallel first then seq
some cubes have no out or in, only up (they are aggregation cubes)
  status is fresh-success, stale-success, fresh-failure, stale-failure, running
debug_log=/tmp/debug
  see detail trace info in separate file
prep_required can be set by various dependencies, and should be searched at the beginning to make sure it is done, before starting the real process
  this isn't really needed, resource management used instead. Cell update fails if resource is unavailable, and user must make it available
  maybe resources can be checked before starting?
support allocating/locking resources (tunnels)
  resource cubes have a dimension that is arbitrary (requestors will ask for `any` of that)
  how to wait for resource to become available if available pool is empty?
    also, the lock must be exclusive, not read only like normal deps
    ( ) make resources different than dependencies (they don't need an update anyway? actually they probably do)
        exclusive lock
        availability=free|in-use|broken
        res/
env/dc/zone/region
  env: e1 e2 e3
  dc: ipc1 ipc2 bom blr
  zone: r1 r2
  region: us india
  shortcuts:
    env=e31i -> env=e3 dc=bom zone=r1 region=india
how can a cube update continue where it left off if it's iterating over a large set of cells?
  (*) need to make skipping over very fast
  ( ) if it's a high volume cube, record successful dim updates in a sorted file and read that file as it goes through the updates to perform
      this is the same as looking at the out folder
how to keep empty dirs in git?
  (*) file placeholders
      if the file exists with the same name, delete the file and create the dir
      touchup process happens only when cube is modified (cube folder mod date > context date)
how to quickly debug an issue?
  an update failed, why?
    check stage it failed in
  make powerful status command that can give not only cube status, but also tell which dep it failed to build, etc
  what about a single failure in the midst of hundreds of successes of the same cube?
    need to be able to associate a cube with a single failure to update a deep dependency
  4 digit build numbers
  log progress for each build:
    last-failure -> update-8329
    %01239 started update of up/etcd-push #4382
inputs of every update should be recorded
if cell update recently failed, must wait until cooldown period has completed before trying again
  calc_cooldown() { } default takes recent_failure_count (in status/) and generates a delay based on that
record timestamps of failures and successes and build ids in execution log
get build id from failures/successes
debug folder stores detailed info
handlers
  look in in/ for folder patterns and execute based on them
  in/src/java compiles to out/main/classes
  in/lib/java pulls in external libs needed to out/lib/java
  in/**.bash-template get hydrated
  sourced before local context
do we really want the folder creation thing?
  (*) folder never auto-created
    - have to check for the folder each time it is read or written
  ( ) all folders always exist
    - only have to update once, no checks later
    - miss flexibility of enabling / disabling features
  ( ) folders get created on demand
----------------------------------------------------------------------------------------------------
can we promote real lock to write lock?
example case
  get etcd value
    path: one-data/etcd/value
    value: env project key 
    derived: service_domain, certs
  single key fetch: one-data/etcd/single-value-loader
  cached version of etcd key: one-data/etcd/dump-loader
  deploy fsm 
    # need to set up tunnels beforehand (maybe 3 tunnels?)
    up
      01-undeploy-function-from-target
        up
          functions-endpoint
      02-deploy-function-to-target
        res
          openshift-controller-tunnel
      03-update-etcd
        up
          etcdctl-setup
            up
              etcd-certs
              etcd-endpoint
      04-confirm-envoy-update
        up
          access-to-envoy-config
      05-undeploy-from-source
        up
          functions-endpoint
folder
  ecp/
    openshift-tunnel/
      
  one-data/
    etcd/
      service-access/
        dim/ 
          # dimension names must be singular, since it can be easily changed to plural later
          env/
            in/
              get_members
                get_members() {
                  members="e1 e2 e3"
                }
            out/
              possibilities?
              functions?
                expand_dim
        in/{e1,e2,e3}
          certs/
            *.crypt
          endpoint/
        out/{e1,e2,e3}
          certs/
          endpoint/
      as-file/
        up/
          server-access/
      function/
        current-version/
          up/
            versioner/
        lambda-json/
          up/
            etcd/key/
        down-dependency/
          up/
            current-version/
            lambda-json/
        up-dependency/
        service-map/
        dependency-service-map/
        shallow-dependencies/
        deep-dependencies/
      key/
    mvs/pod/function/loaded-version/
    mvs/pod/function/is-loaded/
      # if fresh is set, will check out folder against that limit
      # optional (if missing, will NOT be created)
      # this folder timestamp is updated if any deps are modified
      up/
        # 'any' folders. Any of the cubes inside may be used to satisfy dependency
        *?/
      # auto created and updated when a dependency is added with the 'cell add up' command
      down/
      # just like up dependencies, except they are temporarily allocated resources
      res/
      dim/
        {name}/
          # should not do anything cell specific, but may have functions which do
          cube-context
          out/
          out/members  # optional
      # if no in folder, no internal check for freshness
      in/*/
        # special files that are encrypted
        secret/
      # any of these can be vectorized or not
      # is NOT saved in repo
      out/*/
        *.new/
        *.old/
        # is NOT cleared after cell update completes
        secret/
      # where new data should be written to
      # is cleared after cell update completes, created if needed
      # ignored by repo
      tmp/*/
      # if this doesn't exist, logs won't be written
      # logs can be cell or cube level
      log/*/
      # detailed debugging info
      debug/*/
      status/*/
      # if this doesn't exist, locks won't be held. Cell is assumed to be effectively immutable
      # locks can be cube or cell level, selected by cell_logs=t
      lock/*/
      # used to unlock secrets
      key/
      # can't be vectorized because it's a file
      cube-context # used when only status is being checked
        measures="versions"
        expand_dimension() {
          # expand_dimension {dim_folder} {dim_plural} {value}
          # unaliases
          # expands
          # sets defaults
          # returns true or false depending on whether dimension members are valid
        }
        execute_update() {}
debug_log=/aoijdfija/dfoijioajsd
  will output debug trace logs to this folder
help command
  each folder has a README, created from the template
new command  
edit command
status command
list of commands
auto-complete
verify lib behavior
compile
test

