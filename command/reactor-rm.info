name=reactor-dm description="Remove a reactor dependency to a cell." new_command 

name=name \
  description="Name of the reactor to remove. Will list possibilities if not given." \
  add_parameter

command_modifies_context=t
command_modifies_upstream=t
