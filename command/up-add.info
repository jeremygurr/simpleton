name=up-add description="
Add an upstream dependency to a cell. 
" new_command 
name=cell description="Which cell to add the dependency to. Defaults to current cell." add_parameter
name=name description="Name of the dependency to add." add_parameter
name=choices description="Path to each dependency cell choice." add_parameter

