name=clean description="Clean the temporary files (including logs) and cached data of a cell" new_command 

name=kind default=data description="What parts of the cell to clean
    branches = only clean the branches
    data = (default) only clean the cell generated output data. Does not include branches.
    cyto = only clean the cyto folder (all the meta data generated from .dna)
    all = clean data and cyto
    NOTE: clean2 command may be used as a shortcut for 'clean kind=all'" add_parameter


