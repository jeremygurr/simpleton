name=mock-set description="Switched the product of a cell to a mocked result" new_command 

name=mock description="Choose which mock to use. Will ask if none is given.
    Set mock=f to turn off mocking and go back to real cell computation.
  " add_parameter

command_requires_cell=f

