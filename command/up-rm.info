name=up-dm description="Remove an upstream dependency to a cell." new_command 

name=upstream_to_remove \
  description="Name of the dependency to remove. Will list possibilities if not given." \
  aliases='up upstream' \
  add_parameter

