name=get description="Retrieves the cached value of this cell" new_command 

name=form description="Which form to show the data in." \
  choices='simple table kv json raw' \
  default=simple \
  add_parameter

name=fields description="Which fields to display. 
    Defaults to displaying all fields." \
  add_parameter

name=sort description="Which field to sort by. 
    Prefix field name with - for reverse sort." \
  add_parameter

parameter_delayed_questions=fields

