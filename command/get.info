name=get description="Retrieves the cached value of this cell" new_command 

name=form description="Which form to show the data in. 
  Use form=? to get a list of possibilities." add_parameter

name=fields description="Which fields to display.
  Defaults to displaying all fields. Use fields=? to get a list of possibilities.
  If a field is prefixed with <, then the results are sorted in ascending order by that column.
  Use > for descending." add_parameter

