#!/usr/bin/env bash

check() {
  begin_function_flat
    local check_successful=f
    execute_op check || return 1
    if [[ $check_successful == f ]]; then
      out "Check failed"
    else
      out "Check succeeded"
    fi
  end_function
  handle_return
}

